apiVersion: v1
kind: Secret
metadata:
  name: playwright-jsonrpc-secret
  namespace: playwright-jsonrpc
type: Opaque
stringData:
  # IMPORTANT: Replace with your actual API key before deploying
  api-key: "REPLACE_WITH_YOUR_SECURE_API_KEY"
---
# Example using external secrets operator (recommended for production)
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: playwright-jsonrpc-secret
#   namespace: playwright-jsonrpc
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: vault-backend
#     kind: SecretStore
#   target:
#     name: playwright-jsonrpc-secret
#     creationPolicy: Owner
#   data:
#     - secretKey: api-key
#       remoteRef:
#         key: playwright-jsonrpc/api-key
